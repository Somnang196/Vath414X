from common import  setup,work,smooth_scroll
import random, time

if __name__ == "__main__":
  # Only setup driver if selected
    driver = setup("cookie_GabrHalim815")
    # smooth_scroll(driver)
    work(driver,"GabrHalim815","https://x.com/Viviwie6c")